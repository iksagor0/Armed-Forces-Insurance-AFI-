const section_wrapper = document.querySelectorAll(".toggle_section_wrapper");

// if (screen.width <= 768) {
//   section_wrapper.forEach((section) => {
//     const heading = section.querySelector(".afi__footer__title");

//     heading?.addEventListener("click", () =>
//       section.classList.toggle("show_toggle")
//     );
//   });
// }

// const parents = document.querySelectorAll(".toggle_section_wrapper");

section_wrapper.forEach((parent) => {
  parent.querySelector("h2").addEventListener("click", () => {
    parent.classList.toggle("ul_open");
  });
});
