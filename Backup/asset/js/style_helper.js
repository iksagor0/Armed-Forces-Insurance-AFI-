// Insurance Main Section
const mainText = document.querySelector(".main__text__content");
mainText?.parentElement?.classList.add("main_section__container");

// Flood make sound image
const img_parent = document.querySelector(
  ".make_sound_section .landing-page-hero__wrapper"
);
img_parent?.classList.remove("container");
console.log(img_parent);
